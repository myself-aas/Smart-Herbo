import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aas.smartherbo.BAU',
  appName: 'SmartHerbo',
  webDir: 'dist',
  icon: '/assets/icon.png'  
};

export default config;
